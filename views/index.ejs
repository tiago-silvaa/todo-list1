<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.3/font/bootstrap-icons.min.css">

    <title>Todo List</title>

    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <p style="color: white">Navbar improvisada: <a href="/">Home</a> | <a href="/register">Register</a> | 
    <a href="/login">Login</a> | <a href="/todos">Todo</a> | <a href="/dashboard">User Dashboard</a> | 
    </p>

    <div class="text-end">
        <a href="/dashboard" class="btn btn-outline-light btn-lg me-3">User Dashboard</a>
        <button type="button" class="btn btn-outline-light me-2" style="border-radius: 40px;" ><strong>User:</strong> <i class="bi bi-person-circle"></i> <%= usernameDisplay %></button>
        <button type="button" class="btn btn-danger" style="margin-right: 50px; border-radius: 40px;" data-bs-toggle="modal" data-bs-target="#logoutModal">
            Logout <i class="bi bi-box-arrow-left"></i>
        </button>
    </div>
    

    <div class="modal fade" id="logoutModal">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Logout</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to logout?
            </div>
            <div class="modal-footer">
                <form action="/logout" method="POST" class="text-end">
                    <button type="submit" class="btn btn-danger">Logout <i class="bi bi-box-arrow-left"></i></button>
                </form>
                <button type="button" class="btn btn-dark" data-bs-dismiss="modal">Close</button>
            </div>
      
          </div>
        </div>
    </div>

    <% if (message) { %>
        <div class="container mb-3">
            <div class="custom-alert alert alert-<%= message.type %> alert-dismissible fade show col-md-6 mx-auto" role="alert">
                <%= message.content %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% } %>

    <!--Aplicação Todo-->
    <div class="container container1">
        <h1 class="display-4 mb-3 text-center">Todo List <i class="bi bi-list-check"></i></h1>
        <div class="col-md-6 mx-auto form-list-area">

            <!-----------------Formulário para criar tarefa------------->
            <form action="/todos" method="POST">
                <div class="input-group">
                    <input type="text" name="description" class="form-control" placeholder="New task" required>
                    <div class="input-group-append">
                        <select name="priority" class="form-select priorityDropdown">
                            <option value="High">High priority</option>
                            <option value="Medium">Medium priority</option>
                            <option value="Low">Low priority</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary"><i class="bi bi-plus-circle"></i></button>
                </div>
            </form>

            
            <div class="row">
                <div class="col-6">
                    <!--------------Dropdown para filtro por propriedade das tarefas------------->
                    <h5 class="mt-2 mb-2 text-center">Filter by priority:</h4>
                    <div class="col-md-6 mx-auto">
                        <form id="priorityFilter" method="GET">
                            <select id="priority-filter" name="priority" class="form-select">
                                <option value="All">All</option>
                                <option value="High">High</option>
                                <option value="Medium">Medium</option>
                                <option value="Low">Low</option>
                            </select>
                        </form>
                    </div>
                </div>
                <div class="col-6">
                    <!-----------------------------Ordenar tarefas-------------------------->
                    <h5 class="mt-2 mb-2 text-center">Sort:</h4>
                    <div class="mx-auto">
                        <form id="sortFilter" method="GET">
                            <select id="sort-filter" name="sort" class="form-select">
                                <option value="">Sort by...</option>
                                <option value="HighToLow">High to Low Priority</option>
                                <option value="LowToHigh">Low to High Priority</option>
                                <option value="AToZ">A to Z</option>
                                <option value="ZToA">Z to A</option>
                            </select>
                        </form>
                    </div> 
                </div>
            </div>

            <!------------------------Search task filter------------------------>
            <form action="/todos/search" method="GET" class="mt-3">
                <div class="input-group" id="search-filter">
                    <input type="search" name="searchedWord" class="form-control" placeholder="Search task">
                    <button type="submit" class="btn btn-primary" id="searchBtn"><i class="bi bi-search"></i></button>
                    <button type="button" id="clearSearch" class="btn btn-outline-dark">Cancel <i class="bi bi-arrow-90deg-left"></i></button>
                </div>
            </form>


            <!------------------------Lista de tarefas------------------------>
            
            <!--Aviso dinâmico caso a lista estiver vazia-->

            <ul class="list-group mt-2">
                <% tasks.forEach(task => { %>
                    <li class="list-group-item mb-2">

                        <!--Display da descrição-->
                        <span class="<%= task.completed ? 'completed' : '' %> description"><%= task.description %></span>

                        <!--Display da prioridade por tarefa-->
                        <% if (task.priority === 'High') { %>
                            <span class="badge bg-danger"><%= task.priority %></span>
                        <% } else if (task.priority === 'Medium') { %>
                            <span class="badge bg-warning text-dark"><%= task.priority %></span> 
                        <% } else { %>
                            <span class="badge bg-success"><%= task.priority %></span> 
                        <% } %>
                        <button type="button" class="btn btn-outline-primary btn-sm mx-3" data-bs-toggle="modal" data-bs-target="#editPopup<%= task.id %>">
                            <i class="bi bi-pencil-square"></i></button>
                        
                        <!--Botões para completar e apagar tarefa-->
                        <div class="btn-group task-btn-group">
                            <% if (!task.completed) { %>
                                <form action="/todos/<%= task.id %>/complete" method="POST">
                                    <button type="submit" class="btn btn-outline-success mx-1" data-bs-toggle="tooltip" title="Task Completed"><i class="bi bi-check-all"></i></button>
                                </form>
                            <% } else { %>
                                <form action="/todos/<%= task.id %>/notcomplete" method="POST">
                                    <button type="submit" class="btn btn-outline-warning mx-1" data-bs-toggle="tooltip" title="Task Not Completed"><i class="bi bi-arrow-counterclockwise"></i></button>
                                </form>
                            <% } %>
                            <form action="/todos/<%= task.id %>/delete" method="POST">
                                <button type="submit" class="btn btn-danger mx-1" data-bs-toggle="tooltip" title="Delete Task"><i class="bi bi-x"></i></button>
                            </form>
                        </div>             
                    </li>
                <% }) %>
            </ul>

            <!---Modal de editar task---(Desatualizado)--->
            <div class="modal fade" id="editPopup<%= task.id %>" aria-labelledby="editPopupLabel<%= task.id %>" aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h4>Edit Task</h4>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body">
                            <form action="/todos/<%= task.id %>/update" method="POST">
                                <div class="input-group">
                                    <input type="text" name="editDescription" class="form-control flex-grow-1" placeholder="Edit the description" value="<%= task.description %>">
                                    <div class="input-group-append">
                                        <select name="editPriority" class="form-select editPriority">
                                            <option value="High">High priority</option>
                                            <option value="Medium">Medium priority</option>
                                            <option value="Low">Low priority</option>
                                        </select>
                                    </div>                                           
                                    <button type="submit" class="btn btn-primary">Edit Task <i class="bi bi-pencil"></i></button>
                                </div>
                            </form>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Close</button>
                        </div>
                    </div>
                </div>
            </div>

            <% if (tasks.length > 0) { %>
                <div class="btn-group all-tasks-btn-group mt-2">
                    <button type="button" class="btn btn-outline-success" onclick="">All Completed <i class="bi bi-check2-all"></i></button>
                    <button type="button" class="btn btn-outline-warning" onclick="">Incomplete All <br><i class="bi bi-arrow-counterclockwise"></i></button>
                    <button type="button" class="btn btn-outline-danger" onclick="">Delete Completed <br><i class="bi bi-trash3-fill"></i></button>
                    <button type="button" class="btn btn-outline-dark" data-bs-toggle="modal" data-bs-target="#deleteConfirm">Delete All <br><i class="bi bi-trash"></i></button>      
                </div>
            <% } %>

            <div class="modal fade" id="deleteConfirm" aria-labelledby="deleteConfirmLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h4>Warning</h4>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body">
                            <p>Are you sure you want to delete all the tasks?</p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-outline-dark" onclick="">Delete all tasks <i class="bi bi-trash"></i></button>
                            <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Close</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/main.js"></script>
</body>
</html>
